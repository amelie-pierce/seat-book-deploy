import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable React Strict Mode to prevent double API calls during development
  reactStrictMode: false,
  /* config options here */
};

export default nextConfig;
